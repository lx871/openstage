# 交接 2026-08-19 P1/P2

## 本次做了什么
- 修复 SqliteEventStore：改为按 file 打开真实文件（非 :memory: 单例），显式 mkdir、明确 :memory: 分支；沿用 EventStore 物化器，落盘与内存一致
- 网关补齐：SSE 流式解析、AbortSignal 取消、指数退避重试（429/5xx）、offline 镜像 stream/done
- 事件投影修复：message.created 的 stateSnapshotId 绑定、setBranch 分支回滚（tip 快照派生全量）、state.snapshot.created（deltas 增量落盘与 cursor 绑定）、conversation.characterIds 透传、branch.test/state 分支测试
- 新增包：@openstage/inspector（planToReport/whyNotInjected/estimatePlanCost/diffPlans）、@openstage/memory（working/episodic/semantic 三层 + summarize/extractFacts）、@openstage/agent（state.patch/memory.remember/dice.roll + runtime）、@openstage/extensions（能力清单+沙箱主机位）、@openstage/recipe（compat/native 声明式配方与 compileWithRecipe）
- 工具：compat-check（未知字段透传与双模式 diff，P0 信任基石）
- CLI 增强：trace 接 Inspector 报表（归因/预算/断点）、branch 演示、兼容 sqlite 文件持久化的 events 输出；修复 BOM（tsx 报 package.json 解析失败）
- 测试从 14→21，新增 branch/inspector/streaming 三组；tsc clean

## 停在哪里
- 项目已到“全栈骨架可跑、核心能力可验证”阶段；UI（React/Tauri）未开始，真实模型流式仅离线镜像已验证，线上 fetch 路径待接真实 key。

## 下一步建议
1. 接真实 Provider（OPENAI_API_KEY）跑 stream + inspector diff 全链路记录
2. 补 React 头部界面（会话列表、Prompt Inspector 与 whyNot 解释视图）
3. 清理与发布准备（scripts、CHANGELOG、LICENSE、贡献指南约定）

## 已知坑
- PowerShell 默认 UTF-8 BOM 写入；已用 .NET 无 BOM 覆盖修复；新文件请用 write 工具新建以避免 BOM
- tsconfig/vitest 的 paths/alias 需同步维护
